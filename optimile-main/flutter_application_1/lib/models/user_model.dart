import 'package:cloud_firestore/cloud_firestore.dart'; class DeliveryDriver { final String id; final String name; final String email; final String phone; final String role; final DateTime? createdAt; DeliveryDriver({ required this.id, required this.name, required this.email, this.phone = '', this.role = 'driver', this.createdAt, }); Map<String, dynamic> toMap() { return { 'name': name, 'email': email, 'phone': phone, 'role': role, 'created_at': createdAt, }; } factory DeliveryDriver.fromMap(String id, Map<String, dynamic> map) { return DeliveryDriver( id: id, name: map['name'] ?? '', email: map['email'] ?? '', phone: map['phone'] ?? '', role: map['role'] ?? 'driver', createdAt: map['created_at'] != null ? (map['created_at'] as Timestamp).toDate() : null, ); } }